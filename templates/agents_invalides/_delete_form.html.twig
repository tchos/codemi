<form method="post" action="{{ path('app_agents_invalides_delete', {'id': agents_invalide.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agents_invalide.id) }}">
    <button class="btn">Delete</button>
</form>
